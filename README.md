Chat application. 

To run as server:  

`$cargo run -- server` 

To run as client: 

`$cargo run -- client`


Server: 

Multi client server 

Max msg size can be configured currently u32. (MSG_SIZE) 